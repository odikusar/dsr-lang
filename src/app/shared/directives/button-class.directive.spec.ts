import { ButtonClassDirective } from './button-class.directive';

describe('ButtonClassDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonClassDirective();
    expect(directive).toBeTruthy();
  });
});
